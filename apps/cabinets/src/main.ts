// The Pages shell. Picks a cabinet and a tape from fixtures/tapes. Wired once
// the first playable slices land.
const app = document.getElementById('app');
if (app) {
  app.textContent = 'mcp-arcade cabinets: nothing playable yet.';
}
