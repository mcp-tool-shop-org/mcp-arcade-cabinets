# Review — the lead's proofread of the pools that are drawn blind (commit `9aef9a4`)

**Why this review exists.** The proofread landed on `main` on 2026-09-16 and shipped in v0.11.1 without the review from another family that every other content commit had; the review job was orphaned twice that evening. This is that review, owed and paid before the dogfood swarm's first amend wave. **Packet:** the lock G23–G30, the commit's own record (the last section of `docs/vibe-typer.slice4.md`) and the diff from the merge base `b52382a` to `9aef9a4` with docs, changelog, handoff and lockfile omitted; 48 KB. **Reviewers:** Mistral Large 3 (`mistral-large-3:675b-cloud`, answered in about a minute) and Kimi K2.6 (`kimi-k2.6:cloud`, answered in sixteen minutes on the second attempt; the first attempt was killed after fifteen minutes with no output). **Date:** 2026-09-16. Both said halt. The dispositions are the coordinator's; every accepted item is a fix in the swarm's Stage A amend wave (`vibe-sim`), not a separate commit.

## Mistral Large 3

| #   | Item                                                                                             | Disposition  | Why                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------ | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | `agent.hmm` carries "Hmm, I mixed the two columns together.", a piece noun in a pool drawn blind | **accepted** | The hmm pool is drawn with no knowledge of the code the player mistyped; "columns" is a thing a request may not contain. Cut. The hmm, compaction and sync pools join the blind set in the test.                                                 |
| 2–3 | "my cousin is asking for a dragon / a unicorn" name a thing the request did not ask for          | refused      | The creeps are absurd by design (the Director's brief: the user is comically absurd); a cousin asking for a unicorn is true of every request because it is never about the request. The piece rule is for lines that claim to describe the work. |
| 4   | Expand PIECES to "text", "word", "loop", "field", "call", "tool" and the rest                    | refused      | Those are the game's own words and the words of every request; a denylist that wide would empty the pools. Plural forms of the listed nouns are accepted (item 5).                                                                               |
| 5   | The regex misses plurals and possessives                                                         | **accepted** | Plural-aware matching for every listed noun; the columns line above is what it would have caught.                                                                                                                                                |
| 6   | `integrationAsk` does not refuse a tool name carrying a piece noun                               | refused      | Tool names come from the tapes on disk and are already gated by `lineFault`; a tool called "button-tool" is the tape's fact and the field may say it. The integration asks are not a blind pool.                                                 |
| 7   | `drainPerSec` on `sandwich-ledger` is still two ten-thousandths from its cliff                   | acknowledged | Known and owed: a design look at that level, not a re-tune (the pick-up carries it). Not this review's to move.                                                                                                                                  |
| 8–9 | Comments on `POOL_TARGETS` and `MIN_ASKS` do not say what the constants now are                  | **accepted** | Comment accuracy; folded into the amend.                                                                                                                                                                                                         |
| 10  | The voice sheets' rules are not tested                                                           | refused      | The sheets brief the author, not the loader; the mechanical form of their rules is `lines-neutral.test.ts` and the loader's own checks, which are tested.                                                                                        |
| 11  | `lineFault` does not catch piece nouns                                                           | refused      | By design: `lineFault` is the form gate (digits, barred words, length, keyboard); sense is the neutral test's job, and the asks it gates are written from the tool, not drawn blind.                                                             |
| 12  | The integration asks say "the tool"                                                              | refused      | "The tool" is what the request is about on that stack; it is true of every request there.                                                                                                                                                        |

## Kimi K2.6

| #   | Item                                                                                              | Disposition          | Why                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `user.nags` was cut to 14, under the loader floor of 16, so the build fails at load               | refused              | Miscounted from the diff: `user.nags` holds 20 lines on `main` and `pnpm verify` is green. The floors were lowered in the same commit to what a level's draw needs, which the record says.                                                                           |
| 2   | The hmm pool holds "Hmm, I mixed the two columns together." twice, and "columns" evades the regex | **accepted** in part | The line appears once, not twice (checked on `main`); it is cut for the piece noun, as Mistral's item 1.                                                                                                                                                             |
| 3   | PIECES omits "piece", "pieces" and plurals                                                        | **accepted** in part | Plural-aware matching, yes. "Piece" itself, no: it is the game's word for what gets built and is true of every request; the two replies that use it stay.                                                                                                            |
| 4   | "fitting the pieces together" and "making room for the new piece" name a piece                    | refused              | See 3.                                                                                                                                                                                                                                                               |
| 5   | "Nearly there, tucking in the corners." reads as nonsense against a non-UI request                | refused              | It is a completion metaphor, not a claim about a piece; it reads the same under an API request as under a button. The tests domain's audit names this whole class (grammatical, piece-free, still a claim) and it is deferred to an allowlist grammar, a later wave. |
| 6   | The record's keep-list counts do not match the diff                                               | refused              | Checked on `main`: replies 23, nagReplies 15, hmm 16, nags 20, as the record says. The reviewer read the diff's removals as the remaining set.                                                                                                                       |
| 7   | The test does not refuse plurals, so the record's claim is wider than the diff                    | **accepted**         | As 3.                                                                                                                                                                                                                                                                |

**Verdict, both: halt.** **Coordinator's disposition:** three fixes applied in the amend wave (the columns line cut, plural-aware PIECES over a wider blind set, the two comments corrected); the rest refused with reasons above. The larger truth both reviews circled and neither named is in the player's pass of the same evening (`film/players-pass/2026-09-16/notes-main.md`): the reactions by topic are keyed to the code construct rather than the ask, and endless draws per-snippet asks written for another level's product. Those are the swarm's findings C-w1-01 and F-8964cd25, fixed in the same wave.

**What this review cost, kept because it will recur.** Kimi K2.6 through the local daemon takes a quarter of an hour on a 13k-token packet and answers a 15-token probe in two seconds; a 12 KB slice of the same packet did not answer inside three minutes. Mistral Large 3 answered the full packet in about a minute. When a Kimi review is owed and the clock matters, run Mistral beside it and let Kimi's land when it lands.
