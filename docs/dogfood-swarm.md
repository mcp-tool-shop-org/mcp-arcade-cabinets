# The dogfood swarm — the record

**Why.** The Director played v0.11.0 on 2026-09-16 and read nonsense on Vibe Typer's field; the proofread that followed shipped as v0.11.1 the same evening, and his verdict was that the Kimi reviews and the coordinator's briefs had let trash reach a published game: every gate was necessary and none was sufficient. The dogfood swarm of this repo is the answer, with one rule added to every wave and every merge, non-negotiable: **the player's pass.** Before any wave is collected and before any branch merges, the coordinator plays the built Pages bundle and reads what is on the field as a player (every chat line, reply, check-in, toast, standup, on a story level and an endless run, for both cabinets; screenshots and the field's text into `film/players-pass/`), and a second reviewer of a different family reads the game's transcript, not the diff. A line that does not make sense against what was asked is a halt, whatever the tests say.

Control plane: run `swarm-1789612954-a456` (`E:/AI/testing-os/swarms/`), save point `swarm-save-0.11.1` at `73ece39`. Coordinator: Fable, in the main checkout. Builders: Opus in `--isolate` worktrees (Sonnet on ci-tooling), one per domain. Jury: non-Claude, over the local Ollama daemon's cloud tags at no cost.

## Standards compliance (this run)

| Standard                 | Score | Evidence                                                                                                                                                                                                                                                  |
| ------------------------ | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PIN_PER_STEP             | 2     | Every agent carries an explicit model; every review names its model tag and packet; the re-rate stamps its seats; the transcripts are seeded runs (`pnpm transcript`).                                                                                    |
| ANDON_AUTHORITY          | 3     | The player's pass halts on a line, whatever the tests say; `pnpm verify` is law and halted this wave three times (formatting, a removed declaration file, a British spelling) before it passed; the identity scan halts a push.                           |
| NAMED_COMPENSATORS       | 2     | A wave lives on one branch from `main` and is one commit: revert the merge. Worktrees are patches under `.swarm/archive/`. Nothing here publishes; the release path's table is in `docs/npm-launcher.md`.                                                 |
| DECOMPOSE_BY_SECRETS     | 2     | Seven domains, each package owning its own tests; the docs domain is the coordinator's alone; the music pass is its own slice (`docs/music.kickoff.md`).                                                                                                  |
| UNCERTAINTY_GATED_HUMANS | 2     | Stops for the Director: after the health pass with the player's-pass findings, before any spend, before any authoring run, at the release. He set the music pass and the reaction pools' fate mid-run.                                                    |
| EXTERNAL_VERIFIER        | 3     | Severities re-rated by three non-Claude families with the authors' labels stripped; diffs reviewed by Mistral and Kimi from packets cut at the merge base; transcripts read by gpt-oss; the field played by the coordinator, who wrote none of the fixes. |

## The player's pass on main (before any wave)

`film/players-pass/2026-09-16/notes-main.md` has the read. What it found, none of which a test or a diff review had: the user's reactions were keyed to the code construct a snippet teaches, not to the ask (a duck in a sandwich ledger, a parrot for a cat's greeting); endless drew per-snippet asks written for another level's product; a review by product named a thing the level never built; the meeting's lines read as the agent's; the chat header stayed on the first product for a whole endless run; the hint named "the ghost" in the cabinet that is not Ghost. And the Director's own, live: the beds cut off too soon (a flat 36 s hold under 40 s files). Ghost's field text read clean across six tapes and a shift.

## Wave 1 — the audit (Stage A)

Seven Opus auditors (Sonnet on ci-tooling), read-only, 106 findings: 0 CRITICAL, 3 HIGH, 37 MEDIUM, 66 LOW as the authors rated them. Re-rated by `mistral-large-3`, `glm-5.3-flash` and `minimax-m3` with authorship and severity stripped (median of seat ranks; `gpt-oss` returned no JSON, `gemini-3-flash` is retired): 1 CRITICAL, 18 HIGH, 57 MEDIUM, 30 LOW; 52 moved up, 5 down. The coordinator's overrides and reasons are in the run folder (`wave-1/RERATE-overrides.md`); the CRITICAL (the launcher answered any Host and any Origin, so a page the player visits could reach the daemon, the voice bearer and the Anthropic-keyed say seat through DNS rebinding) stood. Five coordinator findings from the player's pass were added beside them.

Wave 2 is an empty amend wave, closed with explicit no-op envelopes: the coordinator called `approve` with positional ids, which approved nothing, and dispatched anyway. Recorded, not hidden.

## Wave 3 — the amend (Stage A)

81 approved findings routed to seven builders in worktrees provisioned with an offline `pnpm install`; 114 files, one branch (`swarm/health-a`), one commit. What landed is the changelog's Unreleased entry. Three builder judgment calls the coordinator ratified: the wave-close loop starts at the open wave (the finding's "+1" would have closed nothing); tape header text is stripped with the screen needles, not the caption needles (the caption set's `ghost` would have painted `server -on-the-menu` on the repo's own tapes); the stdio needle is each server's `initialize` name, because both bundles carry both tool catalogs. One finding reversed by the coordinator: the four `scripts/**/*.d.mts` files are hand-maintained declarations the launcher's tests import, not build output, so they stay tracked.

The vibe-sim builder measured what the coordinator had assumed: the tier reaction pools are not neutral (70 of 108 lines name a piece), so turning the by-topic pool off and falling back to them read worse. The interim, decided on that measurement: while the lever is off, the reaction is one of the generic review lines the lead rewrote in `9aef9a4`, and a level's three reactions and its verdict are four different lines. The two generic lines that asserted a launch mid-level were cut by the lead (twelve became ten; the floor moved with the reason in the code). The tier pools are pinned as a ratchet; **the real fix is an authoring run of per-request reactions, which is the Director's yes.**

**The player's pass on the wave**, before the merge: the full transcripts of every story level at two tiers and three endless runs (`film/players-pass/2026-09-16/vibe-transcripts-health-a.txt`, from the new `pnpm transcript`), Ghost's four rounds, and two runs in the pane (`film/players-pass/2026-09-17/`: a story level and an endless run, the header following the product, the meeting bracketed, the new hint). Every reaction now reads as true of its request. Left for later waves: the template that prefixes `make {product}` to a snippet ask still reads clunky on a plain product ("make a marketplace for dog walks build a comma list of numbers"); the template asks end in a period where the authored ones do not; the transcript runner stamps the meeting's last line with the next beat and prints only the last level's product for an endless run.

**Outside reviews of the wave** (packets per domain, cut from the merge base): Mistral Large 3 read all seven and said halt on each; every concrete claim checked was false against the tree (argument orders match, the listener removal is guarded, the label check is lowercased, the audio-id bound matches the launcher's allowlist, the "unused" imports are used and lint is green) or contrary to house doctrine (the base-branch filter on `pull_request` is removed on purpose). gpt-oss read the wave's transcripts and said halt on six lines: five are the runner's beat stamp on the meeting's last line, one is a product-true review ("the website for my cat understands his brand"); none is a content defect. Kimi K2.6 read the vibe-sim packet (four items, halt): the `make {product}` rewrite matched only a lowercase prefix (accepted, case-insensitive now); the skipped-tape line on stderr printed a file name unfiltered (accepted, non-printable characters replaced); the moved-level test asserted a static key list rather than a draw (accepted, a real `LinePicker` draw now); and "same link as before" and "same page now" were cut for no listed piece (refused: `link` and `page` are both in the denylist, and the cut was the gate's). Its shell read returned empty once and then landed after the merge (six items, halt): a meeting flag not reset at an endless level boundary (accepted, reset in the plan follow); the `.d.mts` deletions (already reversed); a tautological assertion in the bed-list test (accepted, removed); the hint test asserting no "ghost" across the whole mounted field (refused: the wider guarantee is the point and it passes); the debug handle returning the live prefetch clock (refused: a test-only seam whose test writes through it on purpose); and a plan identity check that could rewrite the header every tick (refused: `planOf` returns the run's own plan object, stable until the level changes).

## The Director's play notes on the served bundle, and the answer (between Stage A and Stage B)

Playing the music branch's bundle, the Director reported that enemies rarely attack, that only the boss and now and then a menu sprite shoot, that rounds end short and abruptly, and that the music seems stuck on the opening piece. Measured in the headless sim under a player who holds fire and sweeps: eighteen of a round's twenty-seven sprites died on their entry paths, six ever hovered, and the formation fired zero to six shots in a hundred-and-five-second seat round, because a sprite fired only from the hover, only two of five classes fired, and the first shot waited a whole period. The sim was byte-identical before and after Stage A (twenty-seven runs) and the levers had not moved since the 2026-09-11 tune, so nothing had regressed; the game had been this quiet since then. The music was a real change from the two-minute beds: every tape opens on the inspect wave, the named bed for that wave was taken at the opening, and the hold now follows the bed's own length, longer than any round.

The answer is branch `cabinet/attack-flow` (the full account is `docs/ghost-attack.grok-consult.md`, a consult brief for Grok): four optional levers (`shooters`, `onEntry`, `firstShot` on a formation rhythm; `shotsInFlight` on a rung), the play flowing into the next tape after the scene has held, and, on the music branch, an opening bed drawn by seed from the pool. The Director played the first two tunes and called both too thin; seat is set hot, live warm, hardcore as shipped, and two of the band's seat bars are re-based on the measured numbers with a note beside each.

Reviews of the fix packet (cut from the merge base). Mistral Large 3, eleven points, halt: the three it called major were checked false or by design (tier 3 keeping `firstShot` 1 was the hardcore bar, since re-tuned; `SPAWN_CLASSES` is the closed list the loader validates against on purpose; the cap already reads `meta?.rung.shotsInFlight ?? Infinity`); the rest were comment wording and "no test at the boundary" of the kind the band already measures. Kimi K2.6, three points, halt: `NEXT_TAPE_S` in TypeScript rather than JSON (refused: a named Director constant is the house's other allowed form for a feel number); tier 3 keeping `firstShot` 1 against the changelog's claim (accepted as a wording gap, since re-tuned and the changelog says so); the click-to-replay claim with no handler in the diff (refused: the canvas click that restarts on the scene is the shell's existing handler, `apps/cabinets/src/ghost.ts`, unchanged).

## Wave 5 — the amend (Stage B)

Seven builders in worktrees, all seven envelopes accepted at collect (`Fixed: 34`, no violations); three fixes[] declarations refused (one coordinator id never minted, one declared twice). Merged as patches onto `swarm/health-b` from `main` and verified once, serially, on the merged tree: 1063 tests, the play-throughs green. The shell builder found the seat default already in the tree since `a811b9b` and closed the coordinator's finding by the label ("as recorded (nothing fires)") and tests. Two halves were left to the music branch on purpose and one to the endless builder; the run's owed list carries them.

Reviews (packets per domain from the merge base). Mistral Large 3 said halt on shell (twenty points), vibe-sim (twenty-nine), cabinet-server (thirteen) and launchers (ten) and ship on ghost-sim; every concrete claim checked was false against the tree (the probe back-off doubles after the first retry as its comment says; `writeWord` compares a null `textContent` to a word and writes; the tolerance guard at half is stricter than the comment's one; the comment on a dropped tape names a `TapeError` skip and a rethrow, which is the code), and the rest were "unverified claim in comment" boilerplate. Kimi K2.6 read the shell and cabinet-server packets. Cabinet server, five points, halt: the shooter's tier note carried a digit (accepted, a word now); the Dockerfile and catalog comment said both cabinets read `CABINET_TIER` (accepted, the shooter only); the typing cabinet called a valid tier "not understood" (accepted, it says the variable is the shooter's); `voiceOutcomes` omits `cached` (refused: a cached receipt is an attribute of a voiced take, not a sixth outcome, so the identity holds); the test that pinned the digit (accepted with the note). Shell, three points, halt: a `statechange` listener left on a discarded context (refused: the mount adds the listener only when a score already exists, and a score exists only with the context that built it, so no context is discarded with a listener on it); `AbortSignal.any` and `AbortSignal.timeout` unguarded (refused: the shell targets the browsers Pages serves and the launcher requires Node 22, both of which carry them); the comment on the listener's ownership (refused with the first).

The Director's second play notes, Grok's consult and what shipped from it are in `docs/ghost-attack.grok-consult.md`: three line defects in the sim, the column capped, the shooters unchanged, the reader bot with a gun and a memory, the seat reader bar split, the beds cut on meaning.

The three commits after the first fix packet (the hot tune, then the consult shipped, and the music branch's second) were merged, verified together (1092 tests) and pushed before their outside reviews came back, on the Director's standing word that the served bundle was what he was waiting on; the reviews ran on the pushed diff and their points are dispositioned here, fix-forward if any had stood. Mistral Large 3 on the consult commit, ten points, halt: the two it called logic defects are false (the lane search is a bounded `for` over the field's width; `aligned` tests `target !== undefined` first), the "test cannot fail" points are lower bounds without upper bounds, which is what a floor is, and the rest are comment wording. Mistral on the music commit, nine points, halt: the Dockerfile and catalog comment that the typing cabinet notes `CABINET_TIER` and plays on is what `vibeEnv` does; the `BED_SHORT_S` range assertions pin a lever's shape beside the behavior they sit with; the rest are comment wording and references to dated decisions. Kimi's reads of both packets land here when they do.

**The player's pass on the integrated bundle** (`18e7fa5`, served from the music worktree on port 7791; `film/players-pass/2026-09-17/final-seat*`): two tapes at seat under the sweeping driver. The first (naive-ndjson) played out to its scene at about a hundred and five seconds with three trophies parked and the ship alive; the scene held and the play flowed into the second tape by itself, no click. The second (ollama-intern-mcp.naive-wrap) ended on lamps at thirty-six seconds, which is what the sweeping driver does at seat now, and flowed on again. The beds, by the media elements that started: the first tape opened on the breather bed (a seeded draw, not the inspect bed), took the whisperer's bed the moment the boss came, then the menu's and the doorman's; the second opened on the inspect bed (its own draw), took the whisperer's at the boss, and the third opened on the poison bed. Enemy shots were on the field in ten of forty samples, the formation on it in thirty-six. Every line read on the field (the wave cards, the boss's aside, both end lines) reads against what was on it. A first pass ran on a stale page (the tab had the 01:30 bundle loaded before the rebuild) and read the flow as broken; the lesson, reload the tab after a build, is in memory.

**The Director's heat, and its pass.** He played the consult's tune, called it thin and boring and hardcore weak, and the tune that followed is his: every class fires at every rung above the recorded one, on the way in, the first shot early, the ship capped at three shots (two at hardcore), hardcore's formation every 1.6 seconds with wider spread and a faster dive and boss. He played it and approved it. The band's bars for the rungs that fire were re-based to what the bots measure at that heat, with the reason beside each; the andon bars stand. The pass on that bundle (`film/players-pass/2026-09-17/hot-seat*`): the sweeping driver, which never dodges, lost its lamps at twenty-eight and forty-four seconds on two tapes, enemy shots were on the field in eleven of fourteen samples, the play flowed on both times, and the beds opened on the breather, then inspect, then poison, with the whisperer's bed at the boss.

**The Director's next two asks, the same evening.** Two shots in the air read as weak, and the formation never went to the edges, so a player parked left or right was safe from everything but a dive. Both shipped as levers: a hovering formation now marches across the whole field and comes back in on the far side, one block so its spacing holds (`ladder.json` `sweep`, `sweepPeriod`, none on the recorded rung); and a downed grid lets one of three fire drops fall by a seeded draw over `drops.json` weights, spread as before, rapid (eight shots in the air on a short cooldown for eight seconds) and pierce (a shot keeps going through what it hits), with the rung caps up to four and three. The played pass with a ship parked at the left edge read the first sweep, a march that wrapped around the field, as a kill lane (every sprite crossed the parked column once a period and the field emptied), and the Director's next note was that from the side only the divers and the odd curved shot landed; the sweep became Galaxian's block drift, and the formation aims (`fire.json` formation `aim`, on at every rung that fires). Under aimed fire from a sweeping formation no scripted bot lives out a seated or live tape or finds its lies, so the band's bars for a bot's survival and lie-finding at seat, live and the climb were retired with the reason beside each; the andon bars stand (pinned lies, the recorded rung, the idle floor, the leak scan, the climb's cost, hardcore's deaths), and whether a rung is beatable is the Director's play. The endless slice E1 landed on main the same evening after its own band was re-based to the heat and the sweep, the lead's rank and chain words in its JSON, a headless endless run read as a player (the card, the menu, the tell, the wave and boss lines, the closing scene, the chain word, the code) and two outside reviews of its packet: Mistral's ten points were all "defensive validation would be nice" with no defect named, and Kimi's are dispositioned below when they land.

**Kimi K2.6 on the endless slice, the drops and the aim; Mistral on the aim.** Endless, six points, halt: a comment claimed endless starts at seat where the default is live (accepted, the record says the default is live and the recorded rung is refused); the transcript's header carries the tier, the seed and the call count as digits (refused: the header is the runner's furniture above the screen, the same shape the tape-alone transcript has had since slice two, and the screen scan starts below it); an endless run that hit its call cap was reported as ended by time (accepted: `ended` carries `calls` now, and the runner reads it as a run that ended as it should); a test titled for a comparison it did not make and a comment that overstated how short the runs are (accepted, both reworded). Drops, three points, halt: a helper it read as removed but still used (refused: the last patch removed both the helper and its last use, and lint is green); a piercing shot striking the same hull every frame it is inside it (accepted: a shot remembers what it has struck); the seeded draw collapsing on sprites with no dive index (refused: every sprite gets its index when the round is built). Aim, Mistral, four points, halt: the boss's own `aim` is a separate field (true, and the comment says so); the formation's room is the minimum of its sprites' margins by design, since the block moves as one and its outer sprite is the one that would leave the field; a positive swing spends the right-hand room, which is the direction it moves. Kimi on the aim commit, two points, halt: a diving sprite kept in the formation's room (refused: its home is where it returns to, and the block's extent does not change while it is away); an endless bar's comment naming a number the assertion no longer had (accepted, reworded). Mistral on the lines commit, four points, halt: storage quota and a write race (refused: both writes go through one try and write the same object); a comment on the seed pool (wording); and six voice lines called lock violations for words like "twice", "count" and "files" (refused: the lock's needles are digits and the fact words, which the loader tests on every load, and none of these is one). Kimi on the from-above commit, two points, halt, both wording and both taken: the endless bar's comment credited the lower bound to the aim where the from-above rule was the cause, and a test title said "beside" for a sprite the test parks level with the ship. Mistral on the pace commit found the constants and the page in agreement. The formation's fire from above only (`fromAbove`) landed after the Director's note that a sprite entering from the bottom fired an unavoidable shot from the side. The agent's lines and the bags (`docs/ghost-lines.md`) landed the same hour on the Director's standing rule and were read on two headless transcripts as a player: new cards, asides and catch words landed against their waves, no line out of place.

Kimi K2.6 on the two post-push packets. The consult commit, seven points, halt: the "missing export" is `isHittable`, exported at the foot of `sim.ts` (refused); the reach and entry comments are wording (accepted as wording, left); the three "magic numbers" are the reader bot's aim and step, and the bots are the band's instruments, not the game, so the lever rule does not reach them (refused). The music commit, four points, halt: the boss-at-once tests were said to fail because `whisperer` is not a boss key, and it is the first name in the `isBoss` list (refused); the typing cabinet's tier note said the cabinet has no tiers while `vibeEnv` reads a valid `CABINET_TIER` into its options (accepted: the note reads "not understood; plays at tier zero" again, and the Dockerfile and catalog say both cabinets read it, one and zero as their defaults).

Kimi K2.6 on the lines commit, five points, halt. A repeated line in the doorman's pool and the test it would fail (refused, both: the diff's context line and its retained line are one line; the loader and the test refuse a repeat in any pool, and both are green). `Initialize` as a fact word (refused: the lock's needles are digits and the fact words the gate lists, and the handshake's verb is the agent's own word, not a tool or a model). The piercing shot's comment said a hull was not struck again "while inside" where the set is never cleared (accepted: the comment says once, ever, and why). The bags written only at the scene and on leaving, so a reload mid-round heard the round's lines again (accepted: the shell writes the store on `pagehide` too, and a mount test holds it).

**The reactions run landed** (`docs/vibe-typer.reactions.md`, the Director's yes after wave 5). Two hundred and forty-nine snippets carry a line the user says when that piece ships, written against the request by Mistral Large 3 with the user's voice sheet, read back by glm-5.3-flash (a family that wrote none of them; the script halts if the two families match), and held by a gate to the request's own words. The lead read three levels as a player through `pnpm transcript vibe-typer` (duck rides at tier zero, the sandwich ledger at hardcore, the lost socks at tier one): every line answered the request two lines above it, and the level's last ship says the product's review, which is the sim's design. The builder had counted two frames and left them; the lead reversed that for one of them, since "sits there waiting for me" is true of anything, which is the blind pool's defect in a new coat, and rewrote twenty-nine lines by hand under the same gate. After the rewrite: "sits there" four, "line up" six, "just how i wanted" none. Verified on the merged tree with the standing batch of ghost lines and the two Kimi fixes above.

**The Director's third listen to the beds**, the same hour: they sound right and end after thirty or forty seconds. The verse hold (`BED_SHORT_S`, one evening old) was cutting every two-minute piece at thirty-six at the next wave. Asked, he chose the whole piece: a bed plays through before a wave change may move it, the boss bed still at once. Seventeen more of the agent's lines with it, one per pool.

**The Director's fourth note on the beds, and one on the boss.** The boss appeared too quickly, and the boss music was mostly silent and not daunting. The sim had put the boss on the field at the wave's first beat; asked, he chose the grid fought alone first: a boss enters once its wave's grid is downed, or at half the wave if it still stands, the Whisperer excepted since its grids come from under it (`BOSS_ENTRY_AT`). The music was measured rather than guessed at: the three boss beds open on seven to ten seconds of near silence and carry thin stretches inside, the quiet-layer sections of route M1 being close to nothing on this material. He chose both answers offered: a boss bed starts past its intro now (`BED_ENTRY_S`), and the three are rebuilt loop-first and mastered two LU above the set, in flight in a worktree as this is written, for his ear on the served bundle before they ship. The endless band's idle bar was re-based to the later entry (an idle ship can live past its first call at seat; it never outlasts the reader) and the pilot tests' walk-to-the-boss helper downs the grid on the way. Seventeen more lines, one per pool.

**The playlist.** After his fourth listen of the day, on the bundle with the full-length hold and the boss's own music at once, the Director chose a playlist for the music, because a piece was still being replaced at every boss before it could settle. The rule: the recorded beds are a playlist: a piece runs from its first bar to its last, the next is drawn without repeats until the set is spent, and no event on the field replaces a piece; a boss no longer brings its own music, and the scene between two tapes does not stop the piece. The audio module's wave switch is gone in favor of `keepSong`; the shell keeps a bag in the browser (`ghost.songs`) and hands the next piece to the audio when the playing one ends; the scene between two tapes keeps the piece and stops only the chiptune; the music leaves on the way back to the cabinets. The three rebuilt boss beds landed in the same hour as pieces in the pool (loop-first, no thin sections, mastered above the set; the words gate's one flag on the doorman is a decoder caption loop with no speech in any segment, the class the gate's redefinition names, and route M1 passes by construction). The audio tests were rewritten to the rule (one piece through every wave and boss; the next drawn on the end; the seed's opening; a bag that hands back nothing falls to the chiptune; the scene keeps the piece), and a test comment from the eleventh that had quoted him now carries the decision. Seventeen more of the agent's lines, one per pool.

**The menu piece is out.** Its loop version read as repetitive and harsh as a piece in the playlist; put back to its arrangement for an hour, it went out of the pool altogether on the Director's decision. Seven beds ship; more beds are the next dogfood swarm's, and the menu's candidates stay under `docs/art/` for it.

## Wave 6 — the audit (Stage C, humanization)

Five Opus auditors in worktrees (shell, vibe-sim, ghost-sim, cabinet-server, launchers), read-only, under the dispatcher's humanization lens: what a player or an operator reads, waits on, or reaches by keyboard. The tests and ci-tooling slots have no surface under that lens and were closed with explicit no-op envelopes (the wave collected as failed, was redriven and collected again, which is why the second collect counted every finding as recurring). One hundred and twenty findings: the authors rated twenty HIGH. Re-rated by `mistral-large-3`, `minimax-m3` and `gpt-oss` with authorship and severity stripped (`glm-5.3-flash` timed out; median of seat ranks): 44 HIGH, 70 MEDIUM, 6 LOW, fifty-six moved up and four down. The coordinator's dispositions are in the run folder (`wave-6/RERATE-overrides.md`): six refused for this wave (play on a phone is a feature for the feature pass; the track list versus the boss kinds was superseded by the playlist rule; three points on the `call` tool are the endless slice E2's; Ghost's README row is the coordinator's docs domain), a hundred and eight approved. The auditors' ids are minted afresh at collect and the wave had no id map, so the approval list was mapped to the control plane's ids by description before `swarm approve`.

What the audit found, by theme: keyboard traps in both cabinets (Ghost went keyboard-dead after the full-screen button; the typing cabinet had no focusable field); screen readers given the furniture but not the game; the field talking over itself (the boss card landed on the wave card); a lost lamp and a caught drop with no word in any channel; the end scene not saying how the round ended; a transcript that kept only the last frame; endless never threading the line bags and saying a lamp came back when none did; refusals in the cabinet server naming a rule nobody could read; the typing cabinet handing a client its internal tokens; a refused voice bearer invisible on both sides; launcher dead ends with no next step; the typing cabinet's transitional beats one frame long and four chat lines landing on one frame.

## Wave 7 — the amend (Stage C)

Five Opus builders in worktrees, `--skip-verify`, one commit each on their branch; every envelope accepted at collect (`Fixed: 107`, one partial, no violations, the tests and ci-tooling slots closed with no-op envelopes again since the tests domain's one finding was fixed inside the cabinet-server package). Cherry-picked onto `swarm/health-c` from `main` with no conflicts, then the coordinator wired the seams the builders named across domains: the shell reads the sim's new due time on every chat line and binds a tap on Escape to the sim's clear; the typing cabinet's product tool learned the sim's third answer; the mount tests settle a beat before they read the field. The lead rewrote and widened the pools the ghost builder drafted (a word when a lamp is lost by its cause, a word when a drop is caught or ends, an ending that says how the round ended), named the recorded rung for what the menu calls it, and gave both READMEs the six variables the stdio servers read. Verified once, serially, on the merged tree: 1284 tests, both play-throughs, the control plane's receipt recorded. The spelling gate halted the wave once (three British forms in the shell's comments).

Read as a player before the merge (`pnpm transcript`): a Ghost round now holds its wave card for its whole beat and lands the boss card after it; a caught drop and its ending say a word; the end scene says the tape ran out or the lamps did; endless says a lamp came back only when one did and carries the bags across calls. Two Vibe Typer levels read with the request landing a readable beat after the ship and the check-in answered after a gap. Two things seen and left: two drops caught within a second still overwrite each other's word (rare; the boss card's deference could extend to them), and the endless transcript prints the ending and scene lines twice per call (the runner's capture, not the field). The keyboard and screen-reader changes are pinned by the shell's jsdom tests; the on-screen pass on the built bundle waits for the browser pane, which refused the local address after the tab was closed mid-run.

Outside reviews (packets per domain from the merge base). Mistral Large 3 said halt on all five; every point checked was false against the tree or a comment nit: the "forbidden words" it named (end, round, scene, refused, holds, character, plays, run, turns) are not on the list; "score" and "daemon" were not on the surfaces it cited (one control's explanation names the Ollama daemon, which is the thing it configures); the transcript's `why` field is the runner's furniture; the 403 versus 404 split it called wrong is the split the wave made on purpose; the allowlist agreement test is the test that was asked for. Kimi K2.6 on the shell packet, two points, halt, both taken: leaving full screen by the button left focus on the button, where every key after it had a control as its target and none reached the game (the field takes the keys on the way out now, a text box the player is typing in excepted, with a mount test); and the say seat's failure line quoted whatever threw, cleaned of digits, on the chrome row (a script fault says the plain word now). Kimi on the ghost-sim packet, two points: a stored bag whose cursor sits at the pool's length (refused: that is a spent bag, and the next draw reshuffles and counts a cycle, which the comment now says); and the seat's `brokeAt` count in a runner test (refused: the tick count moves before the seat is called, so the third call is the third tick, and the test passes because the code does what it says).

## Wave 8 — the audit (Stage D, visual polish)

Five Opus auditors in worktrees (shell, vibe-sim, ghost-sim, cabinet-server, launchers), read-only, under the dispatcher's polish lens: what is drawn, printed or laid out, and whether it says what it means. The tests and ci-tooling slots closed with no-op envelopes. A hundred and sixteen findings: the authors rated five HIGH. Re-rated by `mistral-large-3`, `minimax-m3` and `gpt-oss` with authorship and severity stripped (`glm-5.3-flash` timed out again; median of seat ranks): 39 HIGH, 52 MEDIUM, 25 LOW, fifty-six moved up and eight down. The coordinator's dispositions are in the run folder (`wave-8/RERATE-overrides.md`): the track list versus the boss kinds rejected for good under the playlist rule (it had recurred three waves); the phone breakpoint's panes deferred to the feature pass with the phone-play item it belongs to; the twenty-five LOW left open; eighty-nine approved, two of them carrying a decision (the field's scale steps are pixel-locked from one times up, the Director's choice; the punctuation and case passes over the authored pools are mechanical, and the lead read the result before the merge).

What the audit found, by theme: the dark theme's controls at a contrast no one could read an edge by; the typing board's columns refusing to shrink between a tablet and half a display; Ghost's captions landing on its own trophy row, bezel glyphs sliding when a neighbor expired, the fog painted beneath everything it fell through, the Menu drawn at two and a half times its hitbox with its top off the field, hazards drawn off the box that hits; help text past eighty columns and a mistyped flag printing the whole usage on top of the diagnostic; halts in four shapes and four refusals under one word; the approval prompt with properties nobody described and bounds the gate would refuse; the operator's notes carrying the raw spec string, and the worker's own error word interpolated onto the bezel; the code gate's refusal naming a bound nobody used; the shelf in two punctuation conventions; the print's labels in three; a round's closing rows printed twice.

## Wave 9 — the amend (Stage D)

Five Opus builders in worktrees, `--skip-verify`, one commit each on their branch; every envelope accepted at collect (`Fixed: 84`; the tests and ci-tooling slots closed with no-op envelopes; one fix declaration named an id the plane never minted and was skipped as a declaration, the fix itself being on the branch). Cherry-picked onto `swarm/health-d` from `main` with no conflicts, then the coordinator wired the seams the builders named across domains: the transcript runner prints the sim's new unit word under the valuation, the container doc says what the env is baked to, the shell's milestone card follows the sim's casing, and four builder files took the formatter. Verified once, serially, on the merged tree: 1374 tests, both play-throughs, the control plane's receipt recorded. The gate ran tests before the build once and the typing cabinet's play test read a stale bundle; the order is noted below as owed.

Read on the built bundle in the browser pane (the on-screen pass Stage C owed as well): Ghost's chrome reads as an actions row and a settings row, the field takes focus and carries a name, a round flows into the next tape on its own, and at a phone's width nothing overflows and the rows wrap; the typing cabinet's editor takes focus and carries a name, the preview carries the product's, the chat's live regions are there, and the console is clean on both. Leaving full screen by the key is pinned by the mount test and not by the pane, which cannot enter full screen.

Outside reviews (packets per domain from the merge base). Mistral Large 3 said halt on all five with eighty-two points; every point checked was one of three kinds: the packet's own fix note read back as a defect (the vibe and cabinet-server packets, whose commit bodies name the state before the fix), a line already as the tree has it (the gate's tolerance sentence, the sweep's one statement of its flags, the exported options type, the six-tools statement, the two env tables, which are identical), or a word taken for an implementation detail (a build halt's "bed" and a serve line's "cabinet" are operator notes; the file 404 names the address the launcher is serving on, which is the point). None taken. Kimi K2.6 on the shell packet, five points, halt, none taken: the menu's blurb and a control's explanation say "lies" (the word rule guards the agent's lines on the field and the words a client reads, and the blurb is the game's premise, said by the lead); the typing cabinet's end card prints the seed (the seed is the replay code, and the number is the thing itself); the seat row prints the model tag the player chose (the wave's own decision, and the tag is the name); and the cabinet's title contains its own name. Kimi on the ghost-sim packet, seven points, halt, one taken: a cues test asserted that three distinct string literals make a set of three, which cannot fail, and the line is gone (the assertions above it already hold each cue to its own name). Refused: the field-fill record is said to miss a kind its type no longer names (the type is the wave kinds and the boss kinds, the archivist among them, and the compiler agrees); the barrel is said to omit the said-row type (it exports it); and the endless call's record fields and the transcript runner's header and footer are said to carry a fact word and digits (the record is what a band test reads and the runner's print is the terminal's, and neither is the field).

## Wave 10 — the feature audit, framed as polish

The Director's direction after Stage D: the feature pass is a polish pass. Five Opus auditors in worktrees (shell, ghost-sim, vibe-sim, cabinet-server, launchers), read-only, under a narrowed lens: what a player who already likes each cabinet would still want, and what an operator who already runs the servers would still want; no new game grammar, no new packages; findings ranked by product risk (harm, then leverage, then hygiene, then style); play on a phone named as the first item. The tests and ci-tooling slots closed with no-op envelopes. Forty-eight proposals, twelve rated HIGH by their authors. The coordinator checked the seven load-bearing claims against the tree before the review and all seven held. Re-rated by `mistral-large-3`, `minimax-m3` and `gpt-oss` with authorship and priority stripped, against a product-risk rubric (`glm-5.3-flash` timed out again; median of seat ranks): 1 CRITICAL, 5 HIGH, 33 MEDIUM, 9 LOW, the seats disagreeing on twenty-nine. The overrides are in the run folder (`wave-10/RERATE-overrides.md`): the two halves of phone play held together at HIGH (the panel split on whether touch controls are new grammar; they are a second way to press the same three buttons), the exported key spent by the say seat held at HIGH, E2's `call` tool held at LOW as the endless slice's own work.

What the audit found, by theme: neither cabinet can be played on a phone though both are dressed for one; the shooter's launcher hands an exported hosted key to the say seat ahead of the local model the player picked, where the typing cabinet's launcher refuses to on purpose; a throw in the shell is a blank page that reloads into itself; the container ignores a stop signal and the image answers nothing to a help flag; an operator's tape overlay is invisible to the `tapes` tool; three things Ghost's sim computes and no channel says (a press refused by the column cap, a hit on a hull that lived, the fog landing); the typing cabinet's seed is not a replay code because the stored weak pairs change the draw; its end card has no way forward; a corpus of two hundred and forty-nine snippets of which the listed levels reach a quarter; the pacing and check-in levers no player can touch; and, under those, a set of operator conveniences and a set of larger features (bring-your-own tapes on three surfaces, routes and the back button, the retro's teaching notes, the page's identity, the launcher CLIs kept as two copies).

**The Director's review (Phase 6, before any code).** The findings went to him ranked in three tiers: take (a player or operator is hurt today), take if the wave has room (leverage), and his call. He took all of it in principle and left the cut to the coordinator's discretion for one day's wave, with everything else deferred and logged. Twenty-four approved for wave 11: phone play (both halves, F-ac1375fd and F-8afa8757), the hosted tier opt-in (F-839c901c), the shell's throw handler (F-48536bf5), the container's signals (F-abb7471a) and help (F-043b17f3), the tape marks (F-49995691), the three Ghost tells (F-3c976306, F-62650cb1, F-21f73279), the pilot's failure memory (F-7b8c7498), the typing cabinet's run code (F-8c29c11d), its end card's again and next (F-acb5625e), its endless starting rung (F-9707e290), its pacing levers (F-53da0063), its Escape event (F-7bebfd4f), the milestone measurement only (F-3d4a029f; the ladder stays), view parity across the two servers (F-3eda692d, F-ebca2e33), the seat's backoff (F-27b7e688), an unset voice address meaning no voice (F-73cdcb17), the playlist's preload (F-83056f2e), the Node floor guard (F-69c9e153) and the tool names in both helps (F-93770c6f; the typing cabinet's README block is the coordinator's and is in this commit).

**Deferred on his word, logged for a later pass:** bring-your-own tapes in the browser (F-c7b2e453), from `npx` (F-9c3b5646) and in an endless run (F-b5181705); hash routes and the back button (F-4759c927); the retro showing a shipped piece's teaching note (F-c7ffa215); the page's icon, theme color, card and manifest (F-1ddadc07; the treatment's); the two launcher CLIs factored into one core (F-4e691438); E2's `call` tool (F-343aac59; the endless slice's, with the cheap measurement of the word against the gate still owed); a listed run carrying on into the next level or the ladder rescaled (the other half of F-3d4a029f, his call once measured); the replay code's menu check width (F-02347b6c, his call); tape-core's calibration and reveal modules with no consumer (F-f93a13e6, his call); a headless check mode for both launchers (F-cff99996); a status route so the page can say why a seat is dark (F-14547f9e); the pack gate's report mode (F-26b712d3); the release loops gated to the pack script's table (F-fabab513); the READMEs' environment tables tested against the help (F-939ad6d1); the corpus port's dry run (F-ebd23617); an endless sweep (F-6c66c8f0); a seated play-through (F-45203d8f); the endless transcript's call boundaries (F-08d3a5cf); the dev server's refusal parity (F-690a43f9); the persona sheet lever (F-7963eec3); the pilot's skip list as a lever (F-e191e622); the fairness band's fourth bot (F-91231126); the voice outcomes reported and the notes said on change (F-d97fe384).

## Owed after Stage A

- Wave 11 (the polish build) and its record; then the final test and the treatment to 0.12.0.
- The twenty-four deferred from wave 10, listed above with their ids, for the pass after 0.12.0; the phone breakpoint's panes (F-e0b91549) ride with phone play.
- From Stage D, owed: `pnpm verify` runs the tests before the build, so a test that spawns a built entry reads the previous bundle (build first, or order the script); the shell's `pushBossVerbs` and the endless runner's said-row formatter and `nag` word are the coordinator's seams the builders named; the golden header literal in `scripts/test/runners.test.mjs`; a `words.deployed` lever beside `valuationUnit`; the bezel's second channel for the power glyphs was skipped (F-2af941a8); the workspace import and the `CABINET` row in the Catalog listing are the Director's decisions (F-0e4a2cb8, and the registry's).
- From Stage C, owed: a caption queue for two drops caught within a second; the endless runner's doubled ending line; the shell passing `onFail` to `askNextIntents`, dropping its own dt clamp and reading the key `onBedFail` carries; the menu calling `shiftTitle(draw)`; the cabinet server setting `BossSay.until`; the transcript runner reusing `watchSaid()`; the two Vibe fixes that reach only as far as the sim (the creep fallback still draws another snippet's row; the corpus fingerprint is exported but not stored beside a best); `CABINET_PIN_GATE=on` in `release.yml` and the catalog's `source.commit` bump at the next release.
- A look at the two record pools the editor could not read (wave 5).
- Cross-domain follow-ups the builders named: the `react` tool should pass the request id it read from `view`; the worker's tests wired into CI and the catalog pin's CI check tightened (`.github/**`); `scripts/play.mjs`'s `whyFailed` shares the fairness band's vacuous ceil; the comment in `packages/ghost-on-the-menu/src/play.ts` naming a stale line of `play.mjs`.
- The music pass landed (`docs/music.md`); the next dogfood swarm adds beds to Ghost's playlist (the menu slot is empty on purpose).
- The standing list from the pick-up: the npm deprecate, the nag tool, the Docker MCP Catalog entry, the pull-path recent-asks ordering, the sandwich-ledger cliff, the reaction heard cold.
