node_modules/
dist/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store

# compiler output (tsconfig.base.json sets declaration: true) co-located
# next to hand-written scripts, not authored source

# 1024 originals for a sprite batch; the cabinet ships 128 px downscales
docs/art/originals-2/

# 1024 originals for Vibe Typer's first art batch; the cabinet ships the fits
docs/art/originals-vibe-1/

# the contact sheets for the piece-tile batch; the cabinet ships the 128 px slices
docs/art/originals-vibe-2/

# the generated portraits for the avatar batch; the cabinet ships the 128 px cuts
docs/art/originals-vibe-3/

# the generated cards and ribbon for the milestone batch; the cabinet ships the cuts
docs/art/originals-vibe-4/

# the flac masters ACE-Step wrote for the bed batch; the cabinet ships the mp3s
docs/art/originals-vibe-beds/

# the originals, stems, arranged masters and fresh auditions for Ghost's
# two-minute beds; the cabinet ships the mp3s under apps/cabinets/public/tracks
docs/art/originals-ghost-beds/

# the same evidence for Vibe Typer's seven beds: the 38 s originals, the
# whole-bar loops cut from their masters, the stems, the arranged and mastered
# pieces and the fresh auditions
docs/art/originals-vibe-beds-2min/

# frames from `pnpm film` (a dev tool, not a test)
film/
site/.astro/

# the game built into the landing site by `pnpm build:play` (CI does this)
site/public/play/
.polyglot-cache.json
.swarm/

.env
.env.*
!.env.example
*.pem

# Claude Code worktrees and local settings
.claude/
docs/art/originals-vibe-5/

# The audition page for the Director's listen: served locally, never shipped.
site/public/audition/
